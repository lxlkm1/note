
# day1

1. IDEA显示项目JDK配置不正确（使用JDK25情况下）
![[Pasted image 20260402085715.png]]

解决方法：暂时使用JDK 24 或者等待idea 升级

2. 怎么设置在IDEA 调整界面大小 
解决方法：使用快捷键 shift + alt + +/- 



3. int 类型 8bit, 为什么范围是-128 到 +127，而不是对称的



# day5

1 为什么这里字符串比较使用equals 而不是 ==
```java

@Override 
public boolean equals(Object obj) { 
if(obj == null) return false; 
if(obj instanceof Person) { 
 Person person = (Person) obj; 
return this.name.equals(person.name) &&  this.age == person.age && this.sex.equals(person.sex); 
} 
return false; 
}

```
== 的意思是比较两者的内存地址，内存地址相同就会返回true, String 基于 Object,而Object 的equals 实现默认使用 == ，但是String 类重写了equals方法，不再基于内存地址，而是去比较字符串内容, 而int 可以不使用 == , 是因为int 是基本类型，没有继承Object, 也没有equals方法，而且这个类型数据不保存内存地址，== 就直接比较大小




