


# day2

1. DUAL表示似乎可以被省略，在 SELECT 1 = 2, 0 = 'abc', 1 = 'abc'; 例子中也可以直接执行 
2. 以下表使用WHERE commission_pct = NULL 指定范围查询出来的结果为空 
```mysql
+-----------+---------+----------------+
| last_name | salary  | commission_pct |
+-----------+---------+----------------+
| Ernst     | 6000.00 |           NULL |
| Fay       | 6000.00 |           NULL |
+-----------+---------+----------------+
   
```
3. = 和 <=> 的区别在哪，当一端有NULL的时候

![[Pasted image 20260403124724.png]]

![[Pasted image 20260403124756.png]]

4. 数据库我们为什么不指定表，再指定条件，再选择字段（觉得不太符合逻辑）
5. ![[Pasted image 20260403141858.png]]
6. 
```bash

SELECT e.last_name, d.department_name, d.location_id, l.city FROM employees e LEFT JOIN departments d  ON e.department_id = d.department_id LEFT JOIN locations l ON l.location_id = d.location  WHERE  e.commission_pct IS NOT NULL;
ERROR 1054 (42S22): Unknown column 'd.location' in 'on clause'
```
